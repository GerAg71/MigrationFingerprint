---
version: 1.0
model: stub (product: claude via bedrock, pinned)
temperature: 0.1
output_contract: src/ai/contracts.py
---
# Propose source->target field mappings

Contract: source layout + target layout -> ranked candidate mappings with confidence. Precision over recall.
