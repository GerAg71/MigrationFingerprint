# Synthetic Sample Data — Seeding Manifest

Populated at MS-1.6 (spec Ch. 25.3). Will document the clean pair
(`PLN-CLEAN-01`) and the seeded-defect pair (`PLN-SEED-01`) with exactly one
deliberate defect per seed failure mode (FM-001…FM-018).

Absolute rule (REQ-031): synthetic data only — fake SSNs (900-xx range),
fake names, 50–200 participant plans.
